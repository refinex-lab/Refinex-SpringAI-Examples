spring:
  application:
    name: spring-ai-minimax

  ai:
    minimax:
      base-url: ${MINIMAX_BASE_URL:https://api.minimax.chat}
      api-key: ${MINIMAX_API_KEY}
      chat:
        options:
          model: abab6.5g-chat
          temperature: 0.7
          max-tokens: 2048